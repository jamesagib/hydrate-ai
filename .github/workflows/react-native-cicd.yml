name: React Native CI/CD

on:
  push:
    branches: [main, master]
    paths-ignore:
      - "**.md"
      - "LICENSE"
      - "docs/**"
  pull_request:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      buildType:
        type: choice
        description: "Build type to run"
        options:
          - dev
          - prod-apk
          - prod-aab
          - ios-dev
          - ios-prod
          - publish-stores
          - all
      platform:
        type: choice
        description: "Platform to build"
        default: "all"
        options:
          - android
          - ios
          - all

env:
  EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
  EXPO_APPLE_ID: ${{ secrets.EXPO_APPLE_ID }}
  EXPO_APPLE_PASSWORD: ${{ secrets.EXPO_APPLE_PASSWORD }}
  EXPO_TEAM_ID: ${{ secrets.EXPO_TEAM_ID }}
  GOOGLE_PLAY_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT }}
  NODE_OPTIONS: --openssl-legacy-provider

jobs:
  check-skip:
    runs-on: ubuntu-latest
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    steps:
      - name: Skip CI check
        run: echo "Proceeding with workflow"

  build-and-deploy:
    needs: check-skip
    runs-on: macos-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📦 Install dependencies
        run: |
          npm ci
          npm install -g eas-cli@latest

      - name: 🔧 Select Xcode 16.2
        run: sudo xcode-select -s /Applications/Xcode_16.2.app

      - name: 🗑️ Clear EAS Cache
        run: |
          echo "Clearing EAS cache to force fresh credential creation..."
          rm -rf ~/.eas-build-local

      - name: 📁 Setup Provisioning Profile
        run: |
          echo "${{ secrets.PROVISIONING_PROFILE }}" | base64 -d > Water_AI_Manually_Created_Profile.mobileprovision
          echo "Provisioning profile created"

      - name: 📱 Setup EAS build cache
        uses: actions/cache@v3
        with:
          path: ~/.eas-build-local
          key: ${{ runner.os }}-eas-build-local-${{ hashFiles('**/package.json') }}
          restore-keys: |
            ${{ runner.os }}-eas-build-local-

      - name: 🔄 Verify EAS CLI installation
        run: |
          echo "EAS CLI version:"
          eas --version



      - name: 📱 Build iOS Development
        run: |
          export NODE_OPTIONS="--openssl-legacy-provider --max_old_space_size=4096"
          eas build --platform ios --profile development --local --non-interactive
        env:
          NODE_ENV: development

      - name: 🔍 Find and Copy Development App
        run: |
          echo "Looking for development app files..."
          find . -name "*.app" -type d
          echo "Current directory contents:"
          ls -la
          echo "Copying development app to expected location..."
          # Find the .app file and copy it to the expected location
          APP_FILE=$(find . -name "*.app" -type d | head -1)
          if [ -n "$APP_FILE" ]; then
            echo "Found app: $APP_FILE"
            cp -R "$APP_FILE" ./app-ios-dev.app
            echo "Copied to ./app-ios-dev.app"
          else
            echo "No .app file found!"
            exit 1
          fi

      - name: ✅ Verify Development App
        run: |
          echo "Verifying development app exists..."
          if [ -d "./app-ios-dev.app" ]; then
            echo "✅ Development app found: ./app-ios-dev.app"
            ls -la ./app-ios-dev.app
          else
            echo "❌ Development app not found!"
            exit 1
          fi

      - name: 📱 Build iOS Production
        run: |
          export NODE_OPTIONS="--openssl-legacy-provider --max_old_space_size=4096"
          eas build --platform ios --profile production --local --non-interactive
        env:
          NODE_ENV: production

      - name: 🔍 Find and Copy IPA File
        run: |
          echo "Looking for IPA files..."
          find . -name "*.ipa" -type f
          echo "Current directory contents:"
          ls -la
          echo "Copying IPA to expected location..."
          # Find the IPA file and copy it to the expected location
          IPA_FILE=$(find . -name "*.ipa" -type f | head -1)
          if [ -n "$IPA_FILE" ]; then
            echo "Found IPA: $IPA_FILE"
            cp "$IPA_FILE" ./app-ios-prod.ipa
            echo "Copied to ./app-ios-prod.ipa"
          else
            echo "No IPA file found!"
            exit 1
          fi

      - name: ✅ Verify IPA File
        run: |
          echo "Verifying IPA file exists..."
          if [ -f "./app-ios-prod.ipa" ]; then
            echo "✅ IPA file found: ./app-ios-prod.ipa"
            ls -la ./app-ios-prod.ipa
          else
            echo "❌ IPA file not found!"
            exit 1
          fi

      # - name: 🚀 Submit to App Store
      #   run: |
      #     # Clear any API key environment variables that might interfere
      #     unset APP_STORE_CONNECT_API_KEY
      #     unset APP_STORE_CONNECT_API_KEY_ID
      #     unset APP_STORE_CONNECT_ISSUER_ID
      #     eas submit -p ios --path ./app-ios-prod.ipa --non-interactive
      #   env:
      #     EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
      #     EXPO_APPLE_ID: ${{ secrets.EXPO_APPLE_ID }}
      #     EXPO_APPLE_PASSWORD: ${{ secrets.EXPO_APPLE_PASSWORD }}
      #     EXPO_TEAM_ID: ${{ secrets.EXPO_TEAM_ID }}

      - name: 📦 Upload build artifacts to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: app-builds
          path: |
            ./app-ios-dev.app
            ./app-ios-prod.ipa
          retention-days: 7
